# include <stdio.h>

int main(){

    int x = 1, y=0;
    int z = y || x;

    printf("\nInt values: X=%d, Y=%d\n", x, y);
    // Присвоява се най-лявата стойност, извършват се от дясно на ляво
    x= 1+2,2*3,3+4;
    // Присвоява се най-дясната стойност, извършват се от ляво на дясно
    y=(7*8,8+9,9-4); 
    
    printf("\nX=%d, Y=%d\n", x, y);    
    return 0;
}