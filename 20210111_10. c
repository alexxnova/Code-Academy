#include <stdio.h>

int ramoveBit(int num, int pos);

int main() {
    ramoveBit(171, 3);
    return 0;
    }

int ramoveBit(int num, int pos){
    int ans=num&~(0xFFFFFFFF<<pos);
    printf("%d",ans);
    return 0;
    }
